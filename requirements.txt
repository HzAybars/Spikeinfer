# Versions this engine was developed and verified against.
# For normal use prefer `pip install -e ".[server]"`, which resolves the
# floors declared in pyproject.toml; this file pins the exact set.
torch==2.6.0
transformers==4.57.2
safetensors==0.5.3
numpy==1.26.4

# Server
fastapi==0.124.4
uvicorn==0.33.0
pydantic==2.13.4

# Golden reference + test suite only; the engine never calls snntorch.
snntorch==1.0.0
pytest==8.3.5
httpx==0.27.2

# Triton -- platform dependent, install ONE of:
#   Windows: pip install triton-windows==3.7.0.post26
#   Linux:   bundled with the torch wheel
# Without Triton the package still works: the LIF kernel, the bit-packing and
# the paged spike attention all fall back to pure PyTorch.
